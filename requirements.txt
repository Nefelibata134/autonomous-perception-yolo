# Core dependencies
ultralytics>=8.4          # YOLOv8 detection (auto-installs torch, opencv, numpy, matplotlib, pandas)
deep_sort_realtime>=1.3   # Multi-object tracking (Kalman + ReID)
onnx>=1.17                # ONNX model export
tensorrt>=10.0            # TensorRT inference engine (must match CUDA version)

# Optional: for GIF generation
# pip install Pillow pyav
